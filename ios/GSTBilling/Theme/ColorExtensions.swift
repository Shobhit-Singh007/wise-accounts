import SwiftUI

extension Color {
    static let wiseBlue = Color(red: 0.08, green: 0.39, blue: 0.75)
    static let wiseBlueDark = Color(red: 0.56, green: 0.79, blue: 0.98)
    static let wiseBackground = Color(.systemBackground)
    static let wiseCard = Color(.secondarySystemBackground)
    static let wiseText = Color(.label)
    static let wiseSubtext = Color(.secondaryLabel)
    static let wiseSeparator = Color(.separator)
}
